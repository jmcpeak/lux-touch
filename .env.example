# Database (Neon Postgres) - use a SEPARATE database from wordle
# Create new project at https://console.neon.tech → New Project → name it "lux-touch"
# Copy the connection string and paste below
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Auth (NextAuth v5 / Auth.js)
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# OAuth provider credentials (create apps at provider developer consoles)
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
AUTH_FACEBOOK_ID=""
AUTH_FACEBOOK_SECRET=""

# Cron (for bot jobs)
CRON_SECRET="local-cron-secret"
